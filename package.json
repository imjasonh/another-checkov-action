{
  "name": "checkov-action",
  "version": "1.0.0",
  "description": "GitHub Action to run Checkov security scanner",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node --test src/**/*.test.js"
  },
  "keywords": [
    "actions",
    "checkov",
    "security",
    "iac"
  ],
  "author": "@imjasonh",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
